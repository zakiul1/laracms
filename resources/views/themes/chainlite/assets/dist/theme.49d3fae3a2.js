// Chainlite theme JS (safe stub).
// Put front-end interactivity here. This loads after the page (defer).
// Example: mobile menu toggle
document.addEventListener("DOMContentLoaded", () => {
    // ... your code
});
